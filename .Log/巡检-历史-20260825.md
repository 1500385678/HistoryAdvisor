# 巡检报告 · 34-历史-History · 20260825

> 02:30 自动巡检 · 巡检路径:`/Users/aaron/Mac/Consultant/34-历史-History/_HistoryLib/HistoryWeb/`
> 上次巡检:无(首次建立 .Log 目录)· 本次 commit:见文末

## 巡检项(5 项)

1. **`.Log/` 巡检目录首次建立** —— 此前无任何巡检日志,本次为第一次落库,后续日检将以本目录为基准滚动追加。
2. **Phase 0 仅 1/6 完成** —— `md_to_history_db.py` 已落地(commit `558fc79`),但初始朝代库、人物样本、事件样本、史料样本、飞书 Bot 双流水线 5 项 checkbox 仍为 `[ ]`,未启动入库。
3. **Phase 1 MVP 全未启动** —— Web App 骨架、人物详情页、事件详情页、时间线可视化、Leaflet 地图、跨断代对比、Docker Compose 共 7 项均为 `[ ]`,W5-6 交付存在延期风险。
4. **`.plan/20260824.md` 被删 + 数据文件未追踪** —— 上次 `plan: HistoryAdvisor 20260824` 提交中 `.plan/20260824.md` 已在工作区删除,但 `.plan/20260824-history-data.json`(23KB)未入 git,`git status` 报 `deleted: .plan/20260824.md`,需决定是回退删除还是新增数据文件 commit。
5. **`.gitignore` 第 2 行 `*.log` 误伤 `.Log/` 目录** —— 本机 `core.ignoreCase=true`(macOS 默认),`*.log` 命中大小写不敏感的 `.Log/`,导致巡检目录默认被忽略,本次只能用 `git add -f` 强加,建议在 `.gitignore` 末尾加 `!.Log/` 否定规则或加 `!.gitkeep` 占位,后续 T1 任务顺手修。

## 仓库状态

- **branch**: `main`
- **HEAD**: `87e4de7` `chore: 勾选 Phase 0 第 1 项 (md_to_history_db.py 已完成)`
- **未提交变更**: `deleted: .plan/20260824.md`(49 行)
- **最近 3 次 commit**:
  - `87e4de7` chore: 勾选 Phase 0 第 1 项
  - `558fc79` plan: HistoryAdvisor 20260824
  - `af1be7a` init: HistoryAdvisor v1.0 + 项目开发计划

## 关键文件清单

| 文件 | 大小 | 状态 |
|------|------|------|
| `README.md` | 443 B | 已提交 |
| `项目开发计划.md` | 12.7 KB | 已提交(T1 大计划,巡检不动) |
| `md_to_history_db.py` | 5.1 KB | 已提交(Phase 0 第 1 项产物) |
| `.plan/20260824-history-data.json` | 23 KB | 工作区未追踪 |
| `.plan/20260824.md` | — | 工作区已删除(未提交) |
| `.gitignore` | 50 B | 已提交(忽略 .DS_Store / *.log / node_modules / .venv / __pycache__) |

## 风险与建议

- **R1**: `.plan/20260824.md` 删除与 `.plan/20260824-history-data.json` 出现的因果未明,建议下次 T1 任务前先确认 plan 归档策略(转 JSON 后删 md?还是并存?)。
- **R2**: Phase 0 后 5 项入库任务串行依赖,若 W3 不启动朝代库,人物/事件/史料三项都无 FK 锚点,会连环延期。
- **R3**: Phase 1 7 项全部 `[ ]` + 距 W5-6 截止仅剩 4 周,需 T3 加 API + T4 优化提速,否则 MVP 演示将缺核心可视化。

## 不在本巡检范围

- 不修改 `项目开发计划.md`(T1 大计划,巡检只报告不修改)
- 不执行 `md_to_history_db.py` 重跑(由 T1 任务驱动)
- 不推 push(由 T5 任务统一推送)
