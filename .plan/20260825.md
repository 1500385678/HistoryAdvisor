# 2026-08-25 每日增量 · HistoryAdvisor

> 行业:34-历史-History · Web 项目代号 HistoryAdvisor
> 项目路径:`_HistoryLib/HistoryWeb/`
> 增量执行:T4 每日 03:30(只写 .plan/ 日报 + 轻量代码,不动主计划 `项目开发计划.md`)

---

## 今日选定的未完成项

- **来源**:今日 02:30 巡检报告 `.Log/巡检-历史-20260825.md` 第 5 项
- **条目**:修复 `.gitignore` 第 2 行 `*.log` 误伤 `.Log/` 目录
- **进度**:⬜ → 🟨(本任务)

## 现象

- macOS 默认 `core.ignoreCase=true`,第 2 行 `*.log` 命中大小写不敏感的 `.Log/`
- 导致巡检目录默认被忽略,只能 `git add -f` 强加(见 commit `707c840`)
- 不利于后续 .Log/ 滚动追加,每日巡检都被迫强制 add

## 修复

在 `.gitignore` 末尾加否定规则 `!.Log/`,恢复 `.Log/` 目录追踪。

## 涉及文件

- 修改:`HistoryWeb/.gitignore`(末尾加 1 行)
- 新增:`HistoryWeb/.plan/20260825.md`(本文件,日报主)
- 不动:`项目开发计划.md`(T1 大计划,T4 不越界)
- 不动:`README.md`(已是 v1.0 状态)
- 不动:`md_to_history_db.py`(已是 v0.1,Phase 0 第 1 项已完成)

## 验收

- [x] `.gitignore` 末尾加 `!.Log/`
- [x] `git check-ignore -v .Log/巡检-历史-20260825.md` 返回非零(不再被忽略)
- [x] 不影响其他忽略规则(`.DS_Store` / `node_modules/` / `.venv/` / `__pycache__/` 仍生效)
- [x] 提交后 `git log` 多 1 条 `chore: 修复 .gitignore 让 .Log/ 目录可被追踪` 记录

## 备忘(给 T1 / 后续 T4 看的)

- 02:30 巡检已落地为 commit `707c840`(用 `git add -f` 强加 .Log/ 巡检文件)
- 本次修复后,后续 T4 日报/.Log/ 文件可走 `git add -A` 正常入库,不再需要 `-f`
- `.plan/20260824.md` 工作区已删 + `.plan/20260824-history-data.json` 未追踪,作为历史产物保留,留待后续 T1 决定归档策略(见巡检 R1)
- Phase 0 仍 1/6 完成;Phase 1 7 项全空,加速窗口已开
