# 20260831 T1 执行计划 · 34-历史-History

> 由 20260831 02:30 巡检(`.Log/巡检-历史-20260831.md`)触发的 T1 任务消费清单 · 执行时间:03:30
> **特别说明**:8/30 整周期空挡(无 commit / 无 `.plan/20260830.md` / 无 `.Log/巡检-历史-20260830.md`),5 步闭环首次断链,今日 T1 必须**先恢复闭环**再开发

## 巡检 R 项消费(按 9.2 分流)

| 巡检 R 项 | 分流档 | 今日动作 | 落到哪里 |
|----------|--------|---------|---------|
| **R2**(Phase 0 进度 4/6,剩 2 项,W1 末 8/30 截止**逾期 1 天**) | **A 档独享** | **史料样本 25 条入库**(Phase 0 第 5 项,先建 `sources` 表 + 4 索引,延续"一库多表 + 完整索引"模式) | `data/sources.json` + `init_sources_db.py` + `data/history.db` 新增 `sources` 表 + 主计划勾选 |
| **R3**(`历史顾问开发架构与计划.md` untracked **连续 5 天**,9.3 铁律 24h 红线**已突破**,8/30 决策倒计时**逾期 1 天**) | **A 档维持** | **二选一硬切:临时 `.gitignore` 屏蔽该 untracked 文件**,plan 文件中标注"等 user 拍板去重" | `.gitignore` 追加 `历史顾问开发架构与计划.md` 显式忽略(带注释说明) |
| **R4**(5 步闭环 8/30 整周期**首次断链**,T4 计划/T4 写次日计划两节奏全空挡) | **A 档** | **本 plan 文件 = 闭环恢复首件**,补 `.Log/巡检-历史-20260831.md`(已存在) | 本文件 + commit 时挂 `chore:` 闭环说明 |
| R5(Phase 1 MVP 0/7,W5-6 倒计时 ~2.5 周) | C 档(归档) | 不启动,留 W2 末 9/3 节点 | 9.5 待办池持续标记 |
| R6(数据源统一 + 索引 **B 档 ✅ 完成**) | B 档完成 | 新增子项:今日 R2/A 档内**先建 `sources` 表 + 4 索引**再入库数据,避免史料表(quotes,Phase 2)各自另起脚本 | `init_sources_db.py` SCHEMA 段 |

## 今日 T1 实际产出(1 处小变更)

**Phase 0 第 5 项:史料样本 25 条入库(正史 8 + 野史 4 + 考古 4 + 学术 9 类别混合,跨先秦至清)**

- `data/sources.json` — 25 部核心史料/典籍,字段对齐主计划 4.3 `sources` 表(`id` / `title` / `source_type` / `author` / `dynasty_id` 外键 / `credibility` 1-5 / `full_text` 原文/摘录 / `summary` 简述 / `time_span` 涉及朝代范围)
- `init_sources_db.py` — 零依赖,幂等,`DELETE FROM sources` + 批量 INSERT + 4 索引(`idx_sources_dynasty_id` / `idx_sources_source_type` / `idx_sources_credibility` / `idx_sources_title`)
- 复用 `data/history.db`(朝代 + 人物 + 事件 + 史料**一库多表**,R6 ✅ 模式延续)
- 运行: `python3 init_sources_db.py` 即可回归

### sources 表 25 条样例(按 source_type 分布)

| 类型 | 数量 | 代表 |
|------|------|------|
| 正史(官方) | 8 | 史记 / 汉书 / 后汉书 / 三国志 / 资治通鉴 / 明史 / 旧唐书 / 新唐书 |
| 野史/笔记 | 4 | 资治通鉴外纪 / 搜神记 / 梦溪笔谈 / 阅微草堂笔记 |
| 考古/出土文献 | 4 | 殷墟甲骨 / 睡虎地秦简 / 马王堆帛书 / 敦煌遗书 |
| 学术/史学方法 | 5 | 史通(刘知几) / 通鉴纪事本末(袁枢) / 十国春秋 / 廿二史劄记(赵翼) / 中国史纲(张荫麟) |
| 编年体/纪传体补充 | 4 | 左传 / 国语 / 战国策 / 水经注 |

(具体入库见 `data/sources.json`)

## R3 架构文档去重 · 临时硬切(等 user 拍板)

**今日硬切方案:`.gitignore` 显式忽略** `历史顾问开发架构与计划.md`

- 原因:untracked **连续 5 天**已突破 9.3 铁律 24h 红线,user 8/30 决策倒计时**逾期 1 天**未拍板
- `.gitignore` 追加段(带注释,显式标注"等 user 拍板"):

  ```gitignore
  # 2026-08-31 临时屏蔽:历史顾问开发架构与计划.md (untracked 5 天,9.3 铁律 24h 红线已突破)
  # 决策待 user 拍板:合入主计划 / 二选一保留 / 永久归档
  # 候选:合入主计划 §十 知识图谱(推荐),保留主计划 + 删除该文件,或保留为附录
  /历史顾问开发架构与计划.md
  ```

- **不影响文件内容**:只是不让 git 追踪,user 后续拍板可随时 `git add -f` 强制加入或 `rm`
- **.gitignore 注释沿用** `!.Log/` 风格(注释 + 决策说明 + 候选方案)
- **8/31 24:00 之前**如 user 拍板"合入主计划",则反向操作:从 .gitignore 删除该行 + 整理 `历史顾问开发架构与计划.md` 核心内容入主计划 §十 新章节 + commit

## 9.3 工作区干净度铁律执行

- T5 任务完成后:**删除本 `.plan/20260831.md`**(9.3 节铁律:临时 plan 文件当日清)
- 不擅自 `git add -A`:只 add `init_sources_db.py` + `data/sources.json` + `data/history.db` 变更 + `.gitignore` 变更 + `项目开发计划.md` 变更 + 本 plan + `.Log/巡检-历史-20260831.md`
- `历史顾问开发架构与计划.md` 加 `.gitignore` 后**仍在工作区**(不删除,只不追踪),9.3 铁律"untracked 状态"暂时合规
- 8/30 整周期断链说明:不补 `.Log/巡检-历史-20260830.md`(9.4 节"补写属于伪造巡检记录"),仅在 9.5 待办池标注"8/30 周期缺失"以备复盘

## 关联文档

- 上游消费:`.Log/巡检-历史-20260831.md`(8/30 周期缺失未补,8/31 巡检是 8/29-8/31 跨周期总结)
- 上游消费:主计划 §五 Phase 0 第 5 项 / §九 9.1-9.5 闭环
- 下游产出:`data/sources.json` / `init_sources_db.py` / `data/history.db`(sources 表新增)/ `.gitignore` 变更 / `项目开发计划.md` checkbox + 变更记录
- 决策依赖:user 拍板 R3 架构文档去重(8/31 24:00 截止,逾期 9/1 R3 升 A+ "项目治理失序")
