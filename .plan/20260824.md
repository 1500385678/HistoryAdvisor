# 2026-08-24 每日增量 · HistoryAdvisor

> 行业:34-历史-History · Web 项目代号 HistoryAdvisor
> 项目路径:`_HistoryLib/HistoryWeb/`
> 增量执行:T4 每日 02:30(只写 .plan/ 日报 + 轻量代码,不动主计划 `项目开发计划.md`)

---

## 今日选定的未完成项

- **项目**:项目开发计划.md §5 Phase 0 · 第 1 项
- **条目**:`[ ] 写 md_to_history_db.py:解析 02_历史分支与特点/ + 06_历史大师与学者/ → 史料 JSON`
- **进度**:⬜ → 🟨(v0.1 骨架)

---

## 今日要做

写 `HistoryWeb/md_to_history_db.py` v0.1 骨架,能解析 `02_历史分支与特点.md` + `06_历史大师与学者.md` 里的所有 markdown 表格(分支表 / 时期表 / 大师表 / 名言表),输出到 `.plan/20260824-history-data.json` 作为 Phase 0 第 1 项的工作样本。

## 涉及文件

- 新增:`HistoryWeb/md_to_history_db.py`(v0.1 抽取脚本,目标 50-80 行)
- 新增:`HistoryWeb/.plan/20260824.md`(本文件,日报主)
- 新增:`HistoryWeb/.plan/20260824-history-data.json`(02 + 06 解析输出,作为样本)
- 不动:`项目开发计划.md`(T1 大计划,T4 不越界)
- 不动:`README.md` / `.gitignore`(已是 T1 v1.0 状态)

## 验收

- [x] `md_to_history_db.py` 存在,能 `python3 md_to_history_db.py` 跑通
- [x] 输出 JSON 至少包含 4 类:`branches`(分支)/ `periods`(时期)/ `masters`(大师)/ `quotes`(名言)
- [x] `branches` ≥ 4 条(中史/世界史/专门史/史学史),`masters` ≥ 4 条(司马迁/司马光/梁启超/钱穆),`periods` ≥ 4 条,`quotes` ≥ 4 条
- [x] 不依赖外部库(只用 `re` + `json` + `pathlib`,便于 Phase 1 集成到 FastAPI)
- [x] 提交后 `git log` 多 1 条 `plan: HistoryAdvisor 20260824` 记录

---

## 备忘(给 T1 / 后续 T4 看的)

- **T1 已建主计划**(commit `af1be7a`),6 段 Phase 0 checkbox 全部 ⬜ 未完成。本日 T4 推进第 1 项。
- **T2/T3 后续**:
  - T3(加 API)可基于本脚本的 JSON 写 `GET /api/branches`、`GET /api/masters` 两个 FastAPI endpoint
  - T2(改 bug)等真实 issue 出现后再处理
- **本脚本 v0.1 边界**:
  - 只处理"两列及以上的纯文本表格"
  - 不处理表格里的代码块、嵌套列表
  - 不做朝代库(Phase 0 第 2 项)/ 人物库(第 3 项)/ 事件库(第 4 项)/ 史料库(第 5 项)抽取,留待后续 T4
  - 数据库写入留给 T3(`md_to_db.py` 与本脚本解耦)
