# 20260903 计划 · 历史顾问(补救:R4 闭环断链 + R2/A D3 必落地 + R3/A+ 强制推进)

> 本计划由 9/3 03:30 T5 任务**补救生成**(9/2 23:00 T4 漏写 + 9/3 02:30 巡检 R4 标记闭环断链)
> 消费源:`.Log/巡检-历史-20260903.md` R2/A + R3/A+ + R4/A 三档

## 一、今日目标(2 件大事)

1. **D3 落地**:Phase 0 5.3/6 → 5.5/6(进度 +0.2)
   - `bot/schema.py`:飞书消息卡 JSON 模板(9/2 漏掉,本应与 D2 同时落)
   - `bot/lookup.py`:人物速查流水线(查 figures + 外键回查 dynasties + LIKE 模糊 + 角色优先级)
2. **R3 强制合入**:架构 doc 去重 9/3 触顶,4 天失序,执行 (b) A+ 路径
   - 节选核心 3 段(知识图谱 / 三件套讲解 / 团队建议)入主计划 §十
   - 删除原 `历史顾问开发架构与计划.md`(19 KB untracked → 临时 `.gitignore` 同步回退)
   - `.gitignore` 移除临时硬切行(9.3 铁律 工作区干净回归)

## 二、执行顺序

| 步骤 | 动作 | 预期 commit |
|------|------|-------------|
| 1 | 写 `bot/schema.py` | (本 commit 合并) |
| 2 | 写 `bot/lookup.py` | (本 commit 合并) |
| 3 | 主计划 §十 追加"知识图谱与扩展" | (本 commit 合并) |
| 4 | 主计划 §九.5 标记架构 doc 去重 [x] | (本 commit 合并) |
| 5 | 删除原 19 KB untracked 文件 | (本 commit 合并) |
| 6 | `.gitignore` 回退临时硬切行 | (本 commit 合并) |
| 7 | commit `feat: Phase 0 5.3/6 D3 落地 + R3 A+ 架构 doc 合入主计划 §十` | 1 commit |
| 8 | 推 Gitee | - |
| 9 | 推 GitHub | - |
| 10 | 删本 .plan 文件 | 9.3 铁律 |

## 三、约束

- **D3 实现简洁**:lookup 主体 200-300 行,schema 50-80 行
- **数据保护**:SQL 全部走 `?` 预编译参数化,防注入
- **错误处理**:无结果 / DB 不可用均有友好提示
- **幂等**:同一 query 多次调用不报错
- **9.3 铁律**:本 .plan 当日 T5 commit 后清

## 四、不做什么

- 不启动 D4/D5(明日 9/4 + 后日 9/5)
- 不启动 Phase 1 任何项(W3 9/7 节点再开)
- 不改主计划除 §十 + §九.5 checkbox 之外的内容(巡检不动主计划原则)
